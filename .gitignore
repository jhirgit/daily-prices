# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Note: prices.db is intentionally NOT ignored — the database is committed
# to the repo so it accumulates history across daily runs.

# SPEC-86: rotation_evidence.py's long-history cache (reproducible by re-running it)
data/cache/
