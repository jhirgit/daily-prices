# Dependencies for the MCP server (mcp_server.py) only.
# The daily fetch job uses requirements.txt; keep these separate so the
# Actions cron stays lean.
mcp[cli]>=1.2.0
# get_intraday_quotes fetches live quotes on demand (also used by intraday.py).
yfinance>=0.2.40
